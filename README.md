# micro-service-task-queue
Components: Celery (Consumer / Task Executor), Redis( db), RabbitMQ (Message Broker), Web App (gunicorn / Producer), Flower (Monitoring)

# Note:
The project will run in docker

## Steps:
1. `docker-compose up -d`